#include<iostream>
#include<stdio.h>
using namespace std;
int lista[10], inicio=0, fin=0, cont1=0;
int metbus_lineal(int_lista[], int tama, int busco);
int main();
{
	return 0;
}
int metbus_lineal(int_lista[], int tama, int busco)
{
	int centro,primero=0, ultimo=n-1, valorcentral;
	while(primero<=ultimo)
	{
		valorcentral=(primero+ultimo)/2;
		valorcentral=lista[centro];
		if(busco==valocentral)
		{
			return centro;
		}
		else if(busco<valorcentral)
		{
			ultimo=centro-1;
		}
		else
		{
			primero=centro+1;
		}
	}
	cout<<"\n No esta en la lista \n";
	return -1.0;
}
main()
{
	res='s';
	inicio=0:
	fin=9;
	cout<<"\n Metodo de la busqueda biinaria, \n\t favor de escribir los datos en forma ordenada";
	for(cont1=inicio; cont1<=fin; cont1++)
	{
		cout<<"\n Escribe el elemento[ "<<cont1<<"]:";
		cin>>lista[cont1];
	}
	while(res=='s')
	{
		print f
	}
}

